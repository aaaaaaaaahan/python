CREATE OR REPLACE TABLE FOREXMRG AS
SELECT 
    a.*,
    b.FOREXRATE,
    ROUND(CAST((a.FOREXAMT * COALESCE(b.FOREXRATE,0)) / 0.01 AS INTEGER) * 0.01, 2) AS LEDGERBAL
FROM DPOTH a
LEFT JOIN FOREX b
ON a.CURRCODE = b.CURRCODE
